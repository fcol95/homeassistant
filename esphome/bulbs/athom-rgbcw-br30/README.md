# Athom RGBCW BR30 Smart Bulb - ESP32-C3- ESPHome preflashed
Create a new yaml file for each device by copying "light-athom-br30-example.yaml".
The keys and passwords to replace can be auto-generated by HA esphome builder, by created new device, esp32-c3, and name it accordingly.
Then you can copy the example in their and just keep the generated keys.
You can also set the recovery state of the light by device with light_restore_mode.

## References
Buy Links:
1. https://www.aliexpress.com/item/1005011674824747.html.
2. https://www.athom.tech/blank-1/br30-bulb-for-esphome

See "./manufacturer_config/athom-rgbcw-bulb.yaml" file for the config received from Athom (formatted it only).
Got it from: https://github.com/athom-tech/esp32-configs/blob/main/athom-rgbcw-bulb.yaml.

## Flashing instructions with capture portal of pre-flashed esphome config:
1. Plug bulb into socket.
2. Wait for ESP access point (AP) to show up on your wifi list.
3.1. Should be named "athom-rgbcw-bulb-{part of the mac address}"
3.2. Connect to this AP.
4. Go to webui at 192.168.4.1.
5. Flash with updated esphome config.
    1. Option 1:
        - Enter your WIFI settings in the webui.
        - Once connected to your WIFI and with a valid IP address, flash with:
    ```
    esphome run light-athom-gu10-{name}.yaml --device 192.168.XXX.YYY
    ```
    2. Option 2:
        - Build your new esphome binary (with esphome tool or homeassistant device builder or else).
        - Upload with webui while connected to device access point.
